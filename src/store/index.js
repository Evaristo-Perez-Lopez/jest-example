import { createStore } from "vuex";

export default createStore({
  state: {
    items: [{ name: "pinia", cost: 195.0, quantity: 2 }],
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
