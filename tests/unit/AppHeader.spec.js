import AppHeader from "@/components/AppHeader.vue";
describe("AppHeader", () => {
  test("Si el usuario no está authenticado, no mostrar el boton de salir", () => {
    expect(true).toBe(true);
  });
});
describe("AppHeader", () => {
  test("Si el usuario está authenticado, mostrar el boton de salir", () => {
    expect(true).toBe(true);
  });
});
